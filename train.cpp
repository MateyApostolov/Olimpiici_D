#include <bits/stdc++.h>
using namespace std;

int main () {
    int n, nc;
    cin >> n;
    for(int i = 0; i < 5; i++) {
        stack <int> c, b;
        for(int x = 0; x < n; x++) {
            c.push(x + 1);
            cin >> nc;
            if(nc > ) {
                cout << 0;
                t = false;
            }
            pr = nc;
        }
        if(t) cout << 1;
    }

    return 0;
}
